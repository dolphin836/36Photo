<div class="card-footer">
    <div class="row">
        <div class="col-sm-4 col-xs-12 m-auto">
            <h5><small>{{ page.text }}</small></h5>
        </div>
        
        <div class="col-sm-8 col-xs-12">
            {% if page.link != '' %}
                {% autoescape false %} {{ page.link }} {% endautoescape %}
            {% endif %}
        </div>
        
    </div>
</div>