<thead>
    <tr>
        {% for column in columns %}
        <th scope="col" class="text-center">{{ column }}</th>
        {% endfor %}
        <th scope="col" class="text-center">操作</th>
    </tr>
</thead>

